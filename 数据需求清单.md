# 数据需求清单（跑真实 V46 + 行业共振所需）

> 当前共享盘经全目录核查（根目录 + 代码文件 + 系统文件 + V48_V4修复迭代）确认：
> **仅有** `股票池开盘价_后复权_v3.parquet`（全周期开盘价）、`股票池.xlsx`（行业映射）、
> 以及 `模拟盘*`（仅 2025-07~2026-06 短窗）、`中证1000指数价格后复权.xlsx`（下载被腾讯云拒）。
> **全周期收盘价 / 成交量 / 指数均缺失**，需你上传。

---

## 一、必需文件（跑真实 V46 的最小集合）

| # | 文件内容 | 用途 | 必需度 |
|---|---|---|---|
| 1 | **收盘价（后复权）全股票池** 2020-01 ~ 2026-05 | MACD 信号源（T 日收盘算金叉） | ✅ 必需 |
| 2 | **开盘价（后复权）全股票池** 同区间 | T+1 开盘成交价 | ✅ 必需 |
| 3 | **中证1000指数（后复权）日线** 同区间 | V4 状态机 regime（牛/平/空） | ✅ 必需（或暂用池内等权代理，但真实指数更准） |

> 若你能给一份**长表**（列：日期, 代码, 开盘, 收盘, 最高, 最低, 成交量），1 个文件即可覆盖 #1/#2/#4（成交量可选）。

## 二、可选文件

| # | 文件内容 | 说明 |
|---|---|---|
| 4 | **成交量（全股票池）** | V46 核心信号不依赖成交量；原始 V46 代码读了它，若要完全对齐原始行为则提供 |

## 三、无需再上传

- `股票池.xlsx`（申万行业映射，盘内已有，2261 只全覆盖）
- `模拟盘*.xlsx`（仅 2025-07~2026-06 短窗，对 2020-2026 回测无用）
- `股票池开盘价_后复权_v3.parquet`（已在用，全周期开盘价）

---

## 四、格式要求（任意一种均可，我写适配器）

- **宽表**：index=日期，columns=股票代码（如 `600109.SH` / `000001.SZ`），值为价格。
- **长表**：列含 `日期, 代码, 开盘, 收盘`（+最高/最低/成交量）。
- **扩展名**：`.xlsx` / `.csv` / `.parquet` 均可。
- **复权方式**：后复权（hfq）优先；若是不复权/前复权，请注明，我按你的口径处理。
- **时间覆盖**：建议从 **2019 年或更早** 起，到 **2026-05** 止——因为 MA144 需要约 144 个前置交易日，信号区间从 2020-06 起算。

## 五、对接方式

上传到项目盘后，告诉我：
1. 文件名 / 在盘内的位置；
2. 是宽表还是长表、是否后复权；
3. 股票代码列的格式（带 `.SH/.SZ` 后缀，还是纯数字）。

我会改造 `v46_real_backtest.py` 的装载层直接读取你的文件，跑出**与你自有数据一致的真实 V46 baseline + 行业共振对比**，替代当前新浪重建的近似值。

---

## 六、当前新浪重建（已可用，待复核）

若暂时无法提供自有数据，现有新浪重建结果可作参考（经常量缩放校验，信号时序一致）：
- `/workspace/data/stock_close_hfq.parquet`, `stock_open_hfq.parquet`, `stock_volume_hfq.parquet`, `zz1000_hfq.parquet`
- `/workspace/v46_real_backtest.py` + `/workspace/v46_real_results.json`
- 真实口径 baseline：+533.83% / 15.76% 回撤 / 比率 33.86
